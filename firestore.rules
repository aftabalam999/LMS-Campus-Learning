service cloud.firestore {
  match /databases/{database}/documents {
    // Simple rule: All authenticated users can read and write all data
    // Access control is handled at the application level based on user roles
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}